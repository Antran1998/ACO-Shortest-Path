\section{Dataset / Benchmark maps (Ân)}

Describe the benchmark maps used for evaluation:
\begin{itemize}
  \item Source: project maps in the repository (e.g. \texttt{maps/map1.txt}, \texttt{maps/map2.txt}, \texttt{maps/map3.txt}).
  \item Map format: ASCII grid with markers: \texttt{S} (start), \texttt{F} (goal), \texttt{E} (empty/free), \texttt{O} (obstacle).
  \item Evaluation protocol: run each configuration multiple times (e.g. 20 runs), record min/mean/std of path length (Euclidean), runtime, and success rate.
  \item Preprocessing: conversion to occupancy map and node graph (diagonal connectivity allowed when free).
\end{itemize}